import { AppDataSource } from '../../../../ormconfig';
import { Ticket } from '../../../database/entities/Ticket';

type ReadOneTicketRequest = {
    id: number;
};

export class ReadOneTicketService {}
